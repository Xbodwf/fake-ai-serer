version: '3.8'

services:
  phantom-mock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phantom-mock
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=7143
